import * as React from 'react';
import {useState} from 'react';
import {ChatContainer} from "./MainChat";


const MainChatContainer= (props) => {
    return(
        <ChatContainer/>
    );
}

export default MainChatContainer;